# 内容编辑指南（一页纸）

> 面向内容维护者：**更新网站内容只需编辑 JSON 数据文件，不需要碰任何组件代码。**

## 1. 常用命令

| 操作 | 命令 |
| --- | --- |
| 本地预览（边改边看） | `npm run dev`，浏览器打开 http://localhost:4321 |
| 构建校验（发布前必做） | `npm run build` |
| 预览构建产物 | `npm run preview` |

## 2. 内容文件位置

```
src/
├── content/
│   ├── sections.config.json   ← 板块顺序 / 显隐（一般不用动）
│   ├── zh/                    ← 中文版数据（独立维护）
│   │   ├── profile.json       个人信息（姓名、头衔、单位、简介、邮箱、链接、CV）
│   │   ├── education.json     教育经历
│   │   ├── research.json      研究兴趣
│   │   ├── publications.json  科研成果
│   │   └── awards.json        获奖情况
│   └── en/                    ← 英文版数据（独立维护，可与中文不同）
└── i18n/
    ├── locales.config.json    语言开关（enabled: true/false）
    ├── ui.zh.json             中文界面文案（导航名、按钮文字等）
    └── ui.en.json             英文界面文案
```

**中文版和英文版是两套独立数据**：条目数量、内容可以不同，互不翻译。

## 3. 常见操作

| 想做什么 | 怎么做 |
| --- | --- |
| 新增一条论文/经历/奖项 | 打开对应 JSON 文件，在 `[ ]` 数组里照现有格式复制一段并修改内容 |
| 修改文字 | 直接编辑对应字段，保存即可（dev 服务自动刷新） |
| 删除条目 | 删除对应的 `{ ... }` 对象即可 |
| 替换头像 | 用新文件替换 `public/images/avatar.svg`（或改成自己的图片路径） |
| 中文版/英文版上线不同内容 | 各自编辑 `zh/` 与 `en/` 下的文件，互不影响 |
| 英文版暂不发布 | `locales.config.json` 中把 en 的 `enabled` 改为 `false` |
| 调整板块顺序/隐藏板块 | 编辑 `sections.config.json` 的 `order` / `visible` |
| 修改按钮/导航等固定文字 | 编辑 `src/i18n/ui.zh.json` 或 `ui.en.json` |

## 4. 填写规则（重要）

1. 每条记录必须有 `"id"`，在**同一个文件内**唯一（英文如 `pub-2026-xxx`）。
2. 日期用 `"2025-09"`（年月）或 `"2025"`（年）；"至今"写 `"present"`。
3. 字段留空用 `null`，例如 `"doi": null`、`"cvFile": null`。
4. 字符串必须加英文双引号；注意逗号，除最后一条外每条记录之间要有逗号。
5. 图片放 `public/images/`，PDF/CV 放 `public/files/`，数据中写路径如 `"/files/cv-zh.pdf"`。
6. 科研成果 `type` 只能填：`journal` / `conference` / `patent` / `project` / `preprint`。
7. 本人作者用 `"isSelf": true` 标记（页面会高亮）。

## 5. 发布前检查

- `npm run build` 必须成功：
  - 字段错误 / 缺少文件 → 构建会**失败并报错**，按提示的文件与字段修正；
  - 警告（如界面文案缺 key）→ 补上对应文字。
- 提交后自动部署（按部署平台配置）。
